{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "migrate:dev": {
      "outputs": []
    },
    "migrate:deploy": {
      "outputs": []
    },
    "generate": {
      "outputs": []
    },
    "build": {
      "dependsOn": ["generate", "^build"],
      "outputs": ["dist/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["^build"]
    },
    "start": {
      "dependsOn": ["build"]
    }
  }
}
